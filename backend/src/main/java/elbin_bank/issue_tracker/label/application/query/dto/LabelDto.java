package elbin_bank.issue_tracker.label.application.query.dto;

public record LabelDto(long id,
                       String name,
                       String color,
                       String description) {
}
